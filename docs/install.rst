Install
=======

Army is essentially a Linux tool, there is no support for Windows platforms for now.

Army needs at least python 3.5 to work properly, support for python 2.x has been discarded.
 
.. currentmodule:: army

Army can be installed directly from PyPI::

    pip install army

